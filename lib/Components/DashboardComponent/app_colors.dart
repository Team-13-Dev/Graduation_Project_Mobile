import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xFF0E0F12); // أسود غامق جدًا
  static const Color cardBackground = Color(0xFF1A1C20); // خلفية الكروت

  // Primary Color (الأخضر في التصميم)
  static const Color primary = Color(0xFF00E676);

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF9E9E9E); // رمادي فاتح
  static const Color textFaded = Color(0xFF6D6D6D);

  // Border / Divider
  static const Color divider = Color(0xFF2C2E33);

  // Indicators Colors (للرسوم البيانية)
  static const Color lineChartGreen = Color(0xFF00E676);
  static const Color lineChartBlue = Color(0xFF42A5F5);

  // Wallet Colors
  static const Color walletGreen = Color(0xFF4CAF50);
  static const Color walletOrange = Color(0xFFFFA726);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFB300);
}
